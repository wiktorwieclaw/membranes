[package]
name = "nes_emu"
version = "0.1.0"
edition = "2021"

# See more keys and their definitions at https://doc.rust-lang.org/cargo/reference/manifest.html

[dependencies]
nes_emu_bus = { path = "../nes_emu_bus" }
nes_emu_cpu = { path = "../nes_emu_cpu" }